@model WebApplication1.Models.EmployeeListViewModel

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>RazorDemo</title>
</head>
<body>
    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th></th>
                <th>FirtName</th>
                <th>Lastname</th>
                <th>CityId</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Employees)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.SurName</td>
                    <td>@item.CityId</td>
                </tr>
            }
        </tbody>
    </table>
    <p>@DateTime.Now</p>
    <p>
        3 day ago @{
            string a = (DateTime.Now - TimeSpan.FromDays(3)).ToString();
        }@a
    </p>
    @{
        var numbers = 10;

    }
    <p>@numbers</p>

    @if (numbers < 10)
    {
        <p>Number is less than 10</p>
    }
    else if (numbers >= 10)
    {
        <p>Your number equal or greater than 10</p>
    }
    @switch (numbers)
    {
        case 10:
            <p>Number İs 10</p>
            break;
        default:
            break;
    }

    @for (int i = 0; i < Model.Employees.Count; i++)
    {
        
    }

    @try
    {

    }
    catch (Exception)
    {
        
        throw;
    }

    @using (Html.BeginForm())
    {
        <input type="text" id="" name="" />
        <button type="submit" />
            
    }
</body>
</html>
